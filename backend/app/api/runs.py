"""TODO build guide for batch run routes.

Suggested endpoints:
- `POST /runs/sample`
- `POST /runs/bulk`
- `GET /runs/{id}`

Implementation checklist:
- create sample and bulk runs through services
- return run status and counters
- keep route handlers thin
"""
